# Hardware runs of 18 July 2026 that reached capture (driver text logs)

These are the `mc_kortex` text logs (the driver's stdout, which carries the controller's log) of the
two-arm sessions of 18 July 2026 in which Robot A entered `CaptureTransfer` on the physical arms.
The objective of these sessions was **reach and close**: the object was taped to Robot B's tool, so no
load transfer and no retreat were attempted by design. All nine runs reached that objective.
They complement the binary-log inventory in `../JULY_2026_HARDWARE_LOG_INVENTORY.md`, whose rows of
13:11–13:54 correspond to the same session. Robot A ran with `allowPhysicalExecution: true` and the
physical Robotiq bridge enabled with command and feedback (`controller_config_13-56.yaml`); the transfer
force source was the virtual sensor.

| time | Robot B scenario | how Robot B was driven | Robot A states | ended with | log |
|---|---|---|---|---|---|
| 13:10 | `pure_x` | driver fixed-joint override (always on) | Initial → ObserveObject → SolveInterception → ExecuteCommittedReach → PresentationHold → MovePregrasp → CaptureTransfer → Failure | `[Acquire] hard closure geometry violation` | `simple_physical_pure_x_fixed_b_20260718_131048.mc_kortex.log.xz` |
| 13:35 | `pure_x` | driver fixed-joint override, triggered at DualGiver START | Initial → ObserveObject → SolveInterception → ExecuteCommittedReach → PresentationHold → MovePregrasp → CaptureTransfer → Failure | `[Acquire] hard closure geometry violation` | `exact_event_synced_b_v4_run_20260718_133555.mc_kortex.log.xz` |
| 13:38 | `pure_x` | driver fixed-joint override, triggered at DualGiver START | Initial → ObserveObject → SolveInterception → ExecuteCommittedReach → PresentationHold → MovePregrasp → CaptureTransfer → Failure | `[Acquire] hard closure geometry violation` | `exact_event_synced_b_v4_run_20260718_133855.mc_kortex.log.xz` |
| 13:39 | `pure_x` | driver fixed-joint override, triggered at DualGiver START | Initial → ObserveObject → SolveInterception → ExecuteCommittedReach → PresentationHold → MovePregrasp → CaptureTransfer → Failure | `[Acquire] hard closure geometry violation` | `exact_event_synced_b_v4_run_20260718_133930.mc_kortex.log.xz` |
| 13:56 | `pure_x` | controller references (giver coordinator) | Initial → ObserveObject → SolveInterception → ExecuteCommittedReach → PresentationHold → MovePregrasp → CaptureTransfer → Failure | `[Acquire] hard closure geometry violation` | `real_b_mirrors_simulation_v5_run_20260718_135643.mc_kortex.log.xz` |
| 13:58 | `pure_x` | controller references (giver coordinator) | Initial → ObserveObject → SolveInterception → ExecuteCommittedReach → PresentationHold → MovePregrasp → CaptureTransfer → Failure | `[Acquire] hard closure geometry violation` | `real_b_mirrors_simulation_v5_run_20260718_135816.mc_kortex.log.xz` |
| 14:39 | `ground_near_shifted_more_toward_b` | controller references (giver coordinator) | Initial → ObserveObject → SolveInterception → ExecuteCommittedReach → PresentationHold → MovePregrasp → CaptureTransfer → Failure | `[Acquire] hard closure geometry violation` | `ground_near_shifted_more_toward_b_20260718_143941.mc_kortex.log.xz` |
| 14:40 | `ground_near_shifted_more_toward_b` | controller references (giver coordinator) | Initial → ObserveObject → SolveInterception → ExecuteCommittedReach → PresentationHold → MovePregrasp → CaptureTransfer → Failure | `[Acquire] hard closure geometry violation` | `ground_near_shifted_more_toward_b_20260718_144032.mc_kortex.log.xz` |
| 14:51 | `ground_near_shifted_more_toward_b (z 0.20)` | controller references (giver coordinator) | Initial → ObserveObject → SolveInterception → ExecuteCommittedReach → PresentationHold → MovePregrasp → CaptureTransfer → Failure | `[Acquire] hard closure geometry violation` | `ground_near_shifted_more_toward_b_20260718_145118.mc_kortex.log.xz` |

After the closure, every run ended inside `CaptureTransfer` with the same message: the closure check
against the **virtual** object model (`clear=-0.0000 limiting=left_inner_pad/robot_blue_handle`) reported
the inner pad touching the modelled handle, and the controller entered its fail-safe hold. That is the
expected end of a reach-and-close session: the physical gripper had closed on the physical object (video
evidence in `../PHYSICAL_DUAL_ROBOT_VIDEO_EVIDENCE.md`), the object was fixed to Robot B, and the
controller had no physical contact or force signal to continue the transfer with. `Retreat` was not part
of the objective and was not reached.

How Robot B was driven: in the 13:10–13:39 runs the Kortex driver interpolated Robot B's joints to a
fixed measured target on the presentation time law (`CALL_PHYSICAL_ROBOT_B_FIXED_JOINTS=1`, see
`../../mc_kortex_patch/SOURCE_AUDIT.md`); one such run ended in a Kinova joint fault. From 13:53 the
override was left off and Robot B followed the joint references generated by the giver coordinator,
which is the configuration of this repository.

Two later runs of 13:48 and 13:49 stopped in `ExecuteCommittedReach` and `Initial`; the other logs of the
afternoon (14:07–15:05) did not get past `ObserveObject`, mostly because Robot B could not reach the
start of the lateral scenarios from its base position (see the scenario notes in the two-robot overlay).
